name: dohnet
description: Dart HTTP & WebSocket client with DNS-over-HTTPS (DoH) resolution to bypass DNS poisoning/blocking.
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
